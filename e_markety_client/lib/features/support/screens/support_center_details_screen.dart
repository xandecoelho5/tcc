import 'package:flutter/material.dart';

class SupportCenterDetailsScreen extends StatelessWidget {
  const SupportCenterDetailsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
